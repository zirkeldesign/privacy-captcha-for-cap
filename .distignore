# Hidden directories
.git
.github
.claude
.wordpress-org
.idea
.vscode
.phpactor.json
.gitignore
.distignore
.editorconfig
.version

# Dev tooling
/vendor
phpstan.neon
pint.json
phpunit.xml
phpunit.integration.xml
composer.lock
package.json
bun.lock
bun.lockb
node_modules

# Source-only / build inputs
bin
scripts
tests
.phpunit.cache
get-version.php

# GlotPress readme-translation source (not part of the install)
languages/readme-*.txt

# Distribution artefacts (we're building one — don't ship the last one)
dist

# Docs that aren't part of the install
README.md
CHANGELOG.md
AGENTS.md

# Integration-test WordPress install
.wp-integration

# OS noise
.DS_Store
Thumbs.db

# Written by scripts/upstream-watch.mjs
upstream-report.md
